import cmath

a = int(input('Enter a: '))
b = int(input('Enter b: '))
c = int(input('Enter c: '))

d = b*b-4*a*c

x1 = (-b+cmath.sqrt(d))/(2*a)
x2 = (-b-cmath.sqrt(d))/(2*a)

print('The roots are {0} and {1}'.format(x1,x2))
